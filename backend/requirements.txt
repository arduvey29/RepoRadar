# Lower-bound pins so pip selects prebuilt wheels for the installed Python
# (verified against Python 3.13). Tighten to exact versions before deploy.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27
gitpython>=3.1
radon>=6.0
bandit>=1.7
pip-audit>=2.7
slowapi>=0.1.9
sse-starlette>=2.1.0
groq>=0.11
google-generativeai>=0.8
Pillow>=11.0
pytest>=8.2
pytest-asyncio>=0.23
